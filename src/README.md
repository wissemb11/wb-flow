This directory is reserved for future runtime source code. Currently, `@wbc-ui/wb-flow` is a template-only package — all functionality lives in `bin/install.js` (CLI) and `templates/` (command templates).

Do not add source files here unless a future version transitions the package from template-only to runtime library. If that transition is not planned, this directory can be removed.