# Agentic Identity: my-new-app

## Identity
A modern, lightweight Vue 3 application scaffolded with Vite, serving as the greenfield frontend for the project.

## API Surface
- Primary entry point: `src/main.js` (or `.ts`) mounting to `index.html`.
- Core component tree starting from `src/App.vue`.

## Dependencies
- Internal: None yet (standalone package).
- External: `vue` (^3.5.32), `vite` (^8.0.10).

## Project Identity

This context file defines the my-new-app project for wb-flow. It specifies the project name, stack, and conventions used by all `/wb*` commands.


## Configuration

This context file is read by all `/wb*` commands to understand the project. It defines the project name, tech stack, and workflow conventions.


## Workflow Conventions

All `/wb*` commands in this project follow the conventions defined in this context file. The project uses TypeScript with React and Vitest.

## Related

- [Workflows README](README.md) — Directory structure
- [Demo Root](../../../README.md) — Demo apps overview




## Notes

This context file is read by all `/wb*` commands. Update the stack and conventions here when the project evolves.


## Configuration

This project uses the following wb-flow configuration:
- **Package manager:** npm
- **Test framework:** Vitest
- **Linter:** ESLint with TypeScript rules
- **Formatter:** Prettier

## Related

- [Workflows README](README.md) — Workflow directory structure
- [Demo App Root](../../../README.md) — Project overview

---

